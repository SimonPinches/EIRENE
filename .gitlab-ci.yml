before_script:
    # Install build dependencies
    - apt-get update && apt-get -y install cmake gfortran openmpi

build_gfortran:
   stage: build
   script:
      - mkdir build
      - cd build
      - FC=gfortran cmake ../src
      - make