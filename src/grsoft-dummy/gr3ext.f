c------------------------------------------------------------------------
      SUBROUTINE GR3EXT(AR,IER,EXT)
      IMPLICIT NONE
      INTEGER, INTENT(OUT) :: IER
      REAL, INTENT(OUT) :: AR(*), EXT(3,3)
      RETURN
      END
