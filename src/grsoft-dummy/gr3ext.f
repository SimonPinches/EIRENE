c------------------------------------------------------------------------
      SUBROUTINE GR3EXT(AR,IER,EXT)
      IMPLICIT NONE
      INTEGER, INTENT(OUT) :: IER
      REAL, INTENT( IN OUT) :: AR(*), EXT(3,3)
      IER=0
      RETURN
      END
