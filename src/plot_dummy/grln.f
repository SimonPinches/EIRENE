c-------------------------------------------------------------------------
      SUBROUTINE GRLN(XX,YY,M)
      USE EIRMOD_PRECISION, ONLY: SP
      IMPLICIT NONE
      INTEGER, INTENT(IN) :: M
      REAL(SP), DIMENSION(M), INTENT(OUT) :: XX, YY
      RETURN
      END
